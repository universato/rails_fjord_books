<%= form_with(model: current_user.active_user_relations.find_by(followed_id: @user.id), html: {method: :delete}, local: true) do |f| %>
  <%= f.submit t(:unfollow, scope: %i[dictionary button]) %>
<% end %>
